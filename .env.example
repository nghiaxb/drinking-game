# Canonical production URL for SEO, sitemap, and Open Graph.
# Replace with your real domain before deploy.
VITE_SITE_URL=https://drinking-games.app

# Cheat control channel (Cloudflare Worker). Leave empty to disable the feature entirely.
VITE_CHEAT_SOCKET_URL=
