/** Accent color — matches `--color-accent` in `src/styles/main.css`. */
export const PWA_THEME_COLOR = '#e85d4c'

/** Surface color — matches `--color-surface` in `src/styles/main.css`. */
export const PWA_BACKGROUND_COLOR = '#fef6e8'
