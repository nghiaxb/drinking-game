# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Test
coverage
test-results/
playwright-report/
blob-report/

# Environment
.env
.env.local
.env.*.local

# Cloudflare Worker local dev state (miniflare sqlite) — never commit this
.wrangler/

# Capacitor (future)
icons/
# Derived by `npm run generate:assets` (which prebuild runs), same as public/icons.
# Run that before `generate:cap-assets` on a fresh clone.
assets/icon-only.png
assets/splash.png
android/.gradle
android/app/build
android/build
ios/App/build
ios/App/Pods
ios/DerivedData
